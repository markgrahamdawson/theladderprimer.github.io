{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "start": "nuxt start",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup -d dist --dotfiles"
  },
  "devDependencies": {
    "@nuxtjs/google-fonts": "^3.0.0-0",
    "autoprefixer": "^10.4.12",
    "nuxt": "3.0.0-rc.11",
    "postcss": "^8.4.18",
    "push-dir": "^0.4.1",
    "tailwindcss": "^3.1.8"
  }
}
